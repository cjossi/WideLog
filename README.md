Design and implementation of a scalable data ingestion and analytics pipeline for large-scale expermimental datasets.

The repository contains the data pipeline and analytics layer.
Raw experimental datasets are not included.